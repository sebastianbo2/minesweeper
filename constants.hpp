#pragma once

constexpr inline int SCREEN_WIDTH_TILES { 30 };
constexpr inline int SCREEN_HEIGHT_TILES { 30 };

constexpr inline int TILE_COUNT { SCREEN_HEIGHT_TILES * SCREEN_WIDTH_TILES };

constexpr inline int NUMBER_OFFSET { 8 };
constexpr inline int TILE_WIDTH { 30 };